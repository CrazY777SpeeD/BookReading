@model BookReading.Models.IndexListView

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<style>
    .page-link {
        color: green;
    }
    td{
        font: Algerian;
        font-size: 20px;
        color: green;
     }
    th:hover{
        background-color: red;
    }
</style>
<table class="table table-dark" >
    <thead class="thead-dark" style="background-color: green; color: white;">
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Заголовок</th>
            <th scope="col">Автор</th>
            <th scope="col">Описание</th>
            <th scope="col">Жанр</th>
            <th scope="col">Кол страниц</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var book in Model.books)
    {
        <tr data="@book.Id" onclick="window.location.href='/Book/Details/' + this.getAttribute('data')">
            <td>@book.Id</td>
            <td>@book.Title</td>
            <td>@book.AuthorId</td>
            <td>@book.Description</td>
            <td>@book.Genre</td>
            <td>@book.Pages</td>
        </tr>
    } 
    </tbody>
</table>


<hr/>

<nav aria-label="Page navigation example">
    <ul class="pagination" >
        @foreach (int index in Enumerable.Range(Model.currentPage, 5))
        {
            <li class="page-item">
                @Html.ActionLink(index.ToString(), "List", new {currentPage = index}, new { @class = "page-link" })
            </li>
        } 
    </ul>
</nav>   
